# 작업 정리 — 2026-07-30 (Vercel 정리 · 보안 · 단타봇 · 검색 성능)

> 하루 동안 진행한 리팩터링/버그픽스/성능 최적화 기록. 모두 `main`에 머지·푸시 완료.

## 1. 개요
- **Vercel 완전 제거** (더 이상 사용 안 함) + 배포 흔적 정리
- **치명적 버그 수정**: 텔레그램 발송 경로 크래시
- **보안 강화**: 크론 인증(CRON_SECRET), SECRET_KEY 처리
- **단타 추천 봇** 백테스트 로직 보수화 + RSI 중복 제거
- **운영 사고 2건 대응**: 502(SECRET_KEY), 크론 403(인증 방식 변경)
- **코스피 종목 링크** 형식 수정
- **검색 속도 대폭 개선**: 업비트 **3.85초 → 0.18초**

## 2. 커밋 목록 (main)
| 해시 | 내용 |
|---|---|
| `513979e` | Vercel 흔적 제거 + telegram import 버그 수정 |
| `5fee6c3` | 오래된 README.md 삭제 |
| `57ec5ca` | CRON_SECRET 인증 강제, SECRET_KEY 강화, 백테스트 보수화 |
| `3c9d1cd` | SECRET_KEY 미설정 시 크래시 대신 런타임 랜덤 키(502 픽스) |
| `d66c145` | 코스피 링크 `stock.naver.com/domestic/stock/<code>/price` |
| `69ba84e` | (A) 인프로세스 파싱 캐시 + (B) 기본 스캔 상위150 + 전체토글 |
| `df2d257` | 검색 구간별 소요시간 측정 로그(진단용) |
| `7e6ac49` | (D) 크롤러에서 지표 사전계산 → 웹 계산 스킵 |
| `711ec7f` | (D) 크롤러 저장을 to_json으로 — NaN→null(JSON_VALID 통과) |
| `0555b84` | 티커+시세 목록 30초 캐시 → get_tickers ~0.9s 제거 |

---

## 3. Vercel 제거 & 치명적 버그 (`513979e`)
- **삭제**: `vercel.json`
- **settings.py**: `ALLOWED_HOSTS`/`CSRF_TRUSTED_ORIGINS`에서 `*.vercel.app` 제거(→ `.duckdns.org`), Vercel 관련 주석 정리
- **telegram.py**: `_site_url()`의 `VERCEL_URL` 폴백 제거(→ `SITE_URL`만)
- **scan_views.py / cron_views.py**: `x-vercel-cron` 헤더 인증 제거
- **🔴 치명적 버그**: `scan_views.py`가 `telegram`을 import 하지 않고 `tg.is_configured()`/`tg.send_alert()` 호출 → `send_telegram=1`일 때 `NameError` 크래시. `from .. import telegram as tg` 추가로 해결.

## 4. 보안 강화 (`57ec5ca`, `3c9d1cd`)
### 크론 인증: `?key=wonii` → `CRON_SECRET`
- `cron_scan`, `cron_daily_picks`가 하드코딩 `?key=wonii` 사용 → `?secret=<CRON_SECRET>` 로 통일.
- `?force=true`가 인증을 **우회**하던 취약점 제거(이제 `force`는 시간 필터만 제어).
- ⚠️ **서버 조치 필요**: `.env`에 `CRON_SECRET` 설정 + 크론 호출 URL을 `?secret=...`로 변경(아래 §9).

### SECRET_KEY
- 최초: `DEBUG=False`인데 키 없으면 `ImproperlyConfigured`로 기동 중단(`57ec5ca`).
- → **502 사고 발생**(서버 .env에 키 없었음). 완화(`3c9d1cd`): 키 없으면 **런타임 랜덤 키로 기동 + 경고 로그**(크래시 방지). 단 재시작마다 세션/CSRF 무효화되니 `.env`에 `DJANGO_SECRET_KEY` 고정 권장.

## 5. 단타 추천 봇 백테스트 (`57ec5ca`)
`generate_daily_picks.py`:
- **보수적 승패**: 같은 일봉에서 목표가·손절가 둘 다 닿으면 **손절 우선**(기존엔 무조건 승리로 낙관 왜곡).
- **RSI 중복 제거**: 자체 구현 RSI → `engine.calculate_rsi` 재사용(0 분모 처리 불일치도 해소).
- K값 최적화 14일 창은 유지.

## 6. 코스피 링크 (`d66c145`)
- 결과목록·텔레그램의 코스피 링크를 `https://stock.naver.com/domestic/stock/<종목코드>/price` 형식으로 변경.
- `coin_list.html`, `telegram.py:market_link` 둘 다 반영.

---

## 7. 검색 성능 최적화 ⭐ (`69ba84e`, `df2d257`, `7e6ac49`, `711ec7f`, `0555b84`)

### 진단 (측정 로그로 병목 확정)
- 검색은 **거래소별**로 동작. 병목은 압도적으로 **`scan(calc)` = pandas 지표 계산**(GIL로 1코어 순차). `parse`·`live`(네트워크)는 미미 → **코스피 캐시워밍은 불필요**로 판명.

### 적용한 최적화
- **(A) 인프로세스 파싱 캐시** — 웹서버에 `(ticker,timeframe)->(updated_at, DataFrame)` 유지. 크롤러가 갱신(`updated_at` 변경) 전까진 JSON→DataFrame 재파싱 스킵. (`scan_views._bulk_prefetch_ohlcv`)
- **(B) 기본 스캔 범위 상위 150** — `vol_limit=0`이라도 업비트/코스피(거래대금 `-amount` 정렬 신뢰)면 상위 150만. 빗썸은 정렬 불가라 전체 유지. 전략목록 상단 **`범위: 상위150 / 전체` 토글**(localStorage) → `full=1`로 전체.
- **(D) 크롤러 지표 사전계산** ⭐ — `engine.get_indicator_value`의 계산 블록을 `ensure_indicator_column()`으로 추출, 크롤러가 저장 시점에 활성 전략의 지표 컬럼(MA/EMA/RSI/일목 등)을 미리 채움. 웹은 컬럼이 있으면 계산을 건너뛰고 값만 읽음. (`engine.py`, `update_upbit_cache.py`, `update_kospi_cache.py`)
- **get_tickers 30초 캐시** — 지표 계산이 사라지자 티커+실시간 시세 조회(~0.9s)가 새 병목이 됨. `(exchange,vol_limit)`별 결과를 30초 캐싱해 반복 검색 시 네트워크 왕복 제거. (`scan_views._get_tickers`)

### 🔴 도중 발견한 버그 (`711ec7f`)
- 사전계산 지표 컬럼의 **선행구간 NaN**이 문제. 크롤러가 `df.values.tolist()`로 저장 시 JSON에 `NaN` 토큰 생성 → SQLite `JSON_VALID` **CHECK 제약 위반** → `update_or_create`가 예외 → 크롤러의 `except: pass`가 삼켜 **행이 저장 안 됨**(컬럼이 끝내 안 들어감).
- **해결**: 크롤러 저장을 `json.loads(df.to_json(orient='split'))`로 변경(= `get_ohlcv_with_retry`와 동일). NaN→`null`(유효 JSON), 지표 컬럼 보존, 인덱스 포맷도 ms로 통일.

### 성능 결과 (서버 실측)
| 거래소 | 이전 total | 이후 total | 비고 |
|---|---|---|---|
| **업비트(웜)** | 3.85s | **0.18s** | scan(calc) 2.88→0.13, get_tickers 0.9→0.0 |
| 업비트(콜드) | 3.85s | 1.03s | 첫 검색(티커캐시 채우는 중) |
| **빗썸** | 8.38s | **2.58s** | scan(calc) 7.27→0.44. 이제 parse(1.45s)가 병목 |
| 코스피 | 3.74s | (내일) | 크롤러가 장중 돌면 자동 개선 |

---

## 8. 남은 항목 / 후속
- **코스피 속도**: 코스피 크롤러는 평일 09:00~15:30에만 동작. **다음 장중 크롤 후** 코스피도 사전계산되어 자동으로 빨라짐(scan(calc)↓).
- **측정 로그 유지 중**: `scan_views.py`의 `[SEARCH_TIMING]`/`[PREFETCH_TIMING]` print는 진단용으로 **아직 남겨둠**(내일 코스피 확인용). 확인 후 제거 예정 — 위치: `coin_search_stream` 내부, `_bulk_prefetch_ohlcv` 말미.
- **빗썸 parse(1.45s)**: 원하면 저장 포맷 변경 등으로 추가 최적화 가능(업비트만큼 극적이진 않음).
- **README.md 삭제됨**: 필요시 재작성.
- **`models.W042`**: 7개 모델의 `DEFAULT_AUTO_FIELD` 경고(기능 무해, 원하면 설정으로 정리).

---

## 9. 서버 운영 메모 (GCP, `manage.py runserver` + systemd)
- **프로젝트 경로**: `/home/i_am_psw86/myapp` (`~/coin-screener` 아님)
- **환경변수(.env)**: `settings.py`의 `load_dotenv()`가 이 폴더의 `.env`를 읽음. systemd 유닛엔 `Environment="ALLOWED_HOSTS=*"`만 있음.
  - 필수 설정: `DJANGO_SECRET_KEY`(세션 고정), `CRON_SECRET`(크론 인증), `TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHAT_ID`
- **크론 호출 변경**: cron-job.org URL을 `?key=wonii` → `?secret=<CRON_SECRET>` 로.
  - 코인 스캔: `/cron/scan/?secret=<CRON_SECRET>`
  - 단타 추천: `/cron/daily-picks/?secret=<CRON_SECRET>` (매일 09:00 KST, 신규 등록함)
- **로그 버퍼링 해제**: print가 journalctl에 즉시 보이도록 각 서비스에 drop-in 추가함
  `/etc/systemd/system/<svc>.service.d/unbuffered.conf` → `[Service]\nEnvironment=PYTHONUNBUFFERED=1`
  (대상: coinscreener, upbit-crawler, kospi-crawler)
- **배포 시**: 크롤러 코드가 바뀌면 **크롤러도 재시작** 필요. 지표 사전계산은 **크롤러가 한 사이클(업비트/빗썸 ~15분) 돌아야** DB에 반영됨.
  ```
  cd /home/i_am_psw86/myapp && git pull origin main
  sudo systemctl restart coinscreener upbit-crawler kospi-crawler
  ```
- **진단 명령 모음**:
  ```bash
  # 검색 구간별 시간
  sudo journalctl -u coinscreener -f | grep TIMING
  # 지표 컬럼이 DB에 들어갔는지(업비트 day)
  venv/bin/python manage.py shell -c "from coinscreener.screener.models import OHLCVCache; P=('MA_','EMA_','RSI_','IC_','BB_'); n=sum(1 for r in OHLCVCache.objects.filter(ticker__startswith='KRW-',timeframe='day').iterator() if any(str(c).startswith(P) for c in r.data.get('columns',[]))); print('컬럼 있는 행:', n)"
  # D가 사전계산할 지표 명세
  venv/bin/python manage.py shell -c "from coinscreener.screener.models import Condition; from coinscreener.screener.engine import indicator_specs_by_timeframe as f; print(f(list(Condition.objects.all())))"
  ```
